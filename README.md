# Reverse an array

Reverse an array without reversing special characters

### Example 1

```
Input ['a', '$', 'b', '#', 'c'];
```

```
Output : ['c', '$', 'b', '#', 'a']
```

### Example 2

```
Input : ['b', 'a', '$', 'c', '#']
```

```
Output : ['c', 'a', '$', 'b', '#']
```
